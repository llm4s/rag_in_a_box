<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Console appender with human-readable output (default for development) -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- JSON appender for production (structured logging) -->
    <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="ch.qos.logback.core.encoder.LayoutWrappingEncoder">
            <layout class="ch.qos.logback.contrib.json.classic.JsonLayout">
                <jsonFormatter class="ch.qos.logback.contrib.jackson.JacksonJsonFormatter">
                    <prettyPrint>false</prettyPrint>
                </jsonFormatter>
                <timestampFormat>yyyy-MM-dd'T'HH:mm:ss.SSS'Z'</timestampFormat>
                <timestampFormatTimezoneId>UTC</timestampFormatTimezoneId>
                <appendLineSeparator>true</appendLineSeparator>
            </layout>
        </encoder>
    </appender>

    <!-- Application loggers -->
    <logger name="ragbox" level="${LOG_LEVEL:-INFO}" />

    <!-- llm4s library logging -->
    <logger name="org.llm4s" level="${LOG_LEVEL:-INFO}" />

    <!-- HTTP4s logging (reduce noise) -->
    <logger name="org.http4s" level="INFO" />
    <logger name="org.http4s.ember" level="WARN" />
    <logger name="org.http4s.blaze" level="WARN" />
    <logger name="org.http4s.client" level="INFO" />

    <!-- Database logging -->
    <logger name="com.zaxxer.hikari" level="WARN" />
    <logger name="org.postgresql" level="WARN" />

    <!-- Reduce noise from other libraries -->
    <logger name="io.netty" level="WARN" />
    <logger name="org.flywaydb" level="INFO" />

    <!-- Root logger - use JSON in production, CONSOLE in development -->
    <!-- Set LOG_FORMAT=json for JSON output, any other value for console -->
    <if condition='property("LOG_FORMAT").equals("json")'>
        <then>
            <root level="${LOG_LEVEL:-INFO}">
                <appender-ref ref="JSON" />
            </root>
        </then>
        <else>
            <root level="${LOG_LEVEL:-INFO}">
                <appender-ref ref="CONSOLE" />
            </root>
        </else>
    </if>
</configuration>

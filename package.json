{
  "name": "rag-in-a-box",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare": "husky",
    "lint:admin": "cd admin-ui && npm run lint:check",
    "test:admin": "cd admin-ui && npm run test:run"
  },
  "devDependencies": {
    "husky": "^9.0.0",
    "lint-staged": "^15.0.0",
    "vite-plugin-pwa": "^1.2.0"
  },
  "dependencies": {
    "swagger-ui-dist": "^5.31.0"
  }
}
